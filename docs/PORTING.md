# Porting audit — 2016 → 2026

The complete record of what moved where when these dotfiles were rebuilt on
the topic model (from the 2016 holman fork, through the 2026 chezmoi
intermediate state — preserved in the git tags `legacy` and `chezmoi-v1`).

Policy: **nothing is dropped silently.** Every feature of the 2016 setup and
of the chezmoi rewrite either lives somewhere here, is covered by a native
equivalent, or is listed below with the reason it was dropped. Statuses:
**PORTED** (moved as-is), **RESTORED** (brought back after being lost),
**ADAPTED** (modernized equivalent), **NATIVE** (covered by a default),
**DROPPED** (with reason).

## Shell (zsh)

| Source (era) | Destination | Status | Note |
|---|---|---|---|
| `ZSH_CFG` variable (2016) | `zsh/zshrc.symlink` exports `DOTFILES` | ADAPTED | self-documenting name |
| `TERM=xterm-256color` coercion (2016) | — | DROPPED | modern terminals set it; mutating TERM breaks ssh/tmux |
| HISTFILE in `~/.config` 10k (2016) | `zsh/config.zsh` — XDG state, 50000 | PORTED | chezmoi-era values kept |
| Home/End/Delete/Ctrl-arrow bindkeys (2016) | `zsh/config.zsh` | PORTED | + darwin `\e[H`/`\e[F` variants |
| antigen POST hooks: `fg=9` suggestions, substring-search arrows (2016) | `zsh/plugins.zsh` | PORTED | strict order: autosuggestions → syntax-highlighting → substring-search last |
| antigen + `~/.antigenrc` (2016) | `zsh/install.sh` pinned clones + `zsh/plugins.zsh` | ADAPTED | no manager; drift auto-repaired |
| oh-my-zsh git plugin (2016) | `zsh/git-aliases.zsh` — 44 aliases | RESTORED | `gf` reserved for the personal function; fetch = `gfe` |
| zsh-completions (2016) | `zsh/install.sh` pin 0.35.0, fpath-only | RESTORED | dropped by mistake in the chezmoi rewrite |
| colored-man-pages (2016) | `zsh/plugins/colored-man-pages.plugin.zsh` | PORTED | vendored, sourced by tools.zsh |
| per-directory-history (2016) | `zsh/plugins.zsh` — pinned clone, **Ctrl-G** toggles local/global search | RESTORED | initially dropped in this rebuild; brought back on first use — both histories are always written, the toggle only switches which one Up/Down searches |
| hhighlighter (2016) | — | DROPPED | `grep --color`/`bat` cover it |
| command-not-found plugin (2016) | — | DROPPED | distro-specific, low value |
| debian plugin (2016) | runtime guards in aliases/tools | ADAPTED | fdfind/batcat aliases, Debian fzf fallback |
| rupa/z (2016) | zoxide in `zsh/tools.zsh` | ADAPTED | modern equivalent |
| powerlevel9k multiline theme prefs (2016) | `zsh/config/starship.toml.symlink` | ADAPTED | os/dir/vcs left, status/duration/time right, truncate 4, memory commented (was never active) |
| af-magic-mod light theme (2016) | starship default styling | ADAPTED | two-line prompt feel preserved |
| `colors.zsh` + `spectrum.zsh` (2016) | — | DROPPED | theming infrastructure replaced by starship |
| `window.zsh` title() (2016) | `zsh/window.zsh` | RESTORED | via `add-zsh-hook`, tmux included |
| `reload!`, `cls` (2016) | `zsh/aliases.zsh` | PORTED | |
| `alias vim=nvim` (2016) | `zsh/aliases.zsh` | PORTED | |
| `alias -s pdf=evince` (2016) | — | DROPPED | desktop-Linux era |
| `alias az='\ag'` (2016) | — | DROPPED | silver-searcher dead (→ ripgrep) and AZERTY gone |
| `PROJECTS=~/Code` (2016) → `~/Workspace` | `zsh/env.zsh` | PORTED | current value |
| CC/CXX template data (chezmoi) | `~/.localrc` | ADAPTED | machine variance moved out of the repo |
| nvm lazy-load (chezmoi) | `zsh/env.zsh` | PORTED | |
| `~/.zshenv` XDG + PATH (chezmoi) | `zsh/zshenv.symlink` | PORTED | |
| zprofile brew shellenv (chezmoi) | `zsh/darwin/zprofile.symlink` | PORTED | darwin-only by the OS rule |

## Functions

| Source | Destination | Status |
|---|---|---|
| `c` / `_c` (fuzzy project cd) | `functions/c`, `functions/_c` | PORTED |
| `gf` (checkout -b from origin) | `functions/gf` | PORTED |
| `extract` universal | `functions/extract` | PORTED |
| `_git-rm` completion | `functions/_git-rm` | RESTORED — deleted by accident in 2016 |
| `_boom`, `_brew` (holman) | — | DROPPED — removed by the 2016 fork already |

## Git

| Source | Destination | Status | Note |
|---|---|---|---|
| identity in versioned gitconfig (chezmoi template) | `~/.gitconfig.local` generated by bootstrap | ADAPTED | no personal data in the repo |
| `help.autocorrect`, `apply.whitespace`, `mergetool.keepBackup`, `difftool.prompt` (2016) | `git/gitconfig.symlink` | RESTORED | |
| `wtf` → ruby script (2016) | `bin/git-wtf` + alias | RESTORED | ruby is a runtime dependency (macOS built-in) |
| `rank-contributors` (2016) | `bin/git-rank-contributors` + alias | RESTORED | ruby, idem |
| `up` → `git-up` pull+log (2016) | `bin/git-up`, alias `up = !git-up` | RESTORED | autostash variant remains `glr` |
| `git-promote`, `git-credit`, `git-track`, `git-unpushed`, `git-unpushed-stat`, `git-undo`, `git-amend` (2016) | `bin/git-*` + aliases (`unpushed-stat` new inline) | RESTORED | shellcheck-cleaned, behavior preserved |
| `git-all` (2016) | — | DROPPED | one-liner replaced by direct use |
| `git-nuke`, `git-delete-local-merged` scripts (2016) | inline aliases only | DROPPED (scripts) | the inline chezmoi versions carry the main-branch fix |
| `gitio` (2016) | — | DROPPED | git.io service shut down (2022) |
| `todo`, `search` (ack), `res`, `movieme`, `mustacheme`, `cloudapp`, `atom-*`, `set-defaults` (holman) | — | DROPPED | community gadgets with dead services / editor gone / macOS-era defaults |
| `touchpad_toggle` (2016) | — | DROPPED | desktop-Linux hardware |
| `[hub] protocol` (2016) | — | DROPPED | hub-era |

## tmux

| Source | Destination | Status | Note |
|---|---|---|---|
| base config (prefix C-a, base-index 1, splits with cwd, hjkl panes, HJKL resize, swap windows, reload R, copy-mode v/y) | `tmux/tmux.conf.symlink` | PORTED | |
| `*` synchronize-panes (typo'd + disabled at 2016 HEAD) | restored, fixed + state display | RESTORED | recovered from an intermediate commit |
| `C-c` capture-pane (2016) | restored: mktemp + `$EDITOR` | RESTORED | |
| `C-n` NOTES window (2016, personal path) | restored via private hook `~/.local/bin/tmux-notes` | RESTORED | inert when the hook is absent |
| `M-t` tmuxwords.rb (2016) | restored without ruby: capture + fzf -m → send-keys | RESTORED | |
| `0` ftpane (2016) | modern inline ftpane, fzf-guarded | RESTORED | |
| `p` paste (chezmoi linux branch) | unified runtime try-clipboard | ADAPTED | zero OS branches in the file |
| `theme.sh` tmuxline (2016) | static catppuccin-mocha bar | ADAPTED | the 2016 yellow accent lives on as `#f9e2af` |
| `M-\`` fzf-locate (2016) | — | DROPPED | locate(1) rarely present/current; shell fzf covers file search |
| `M-c` screencapture (2016) | — | DROPPED | iTerm+osascript era; OS-level screenshots |
| `default-shell $SHELL` (2016) | — | DROPPED | tmux resolves it; setting it breaks fresh machines |
| `/` next-layout (2016) | — | DROPPED | was already overridden in 2016 |
| F9/F10 prefix-switching block (2016) | — | DROPPED | "here be dragons" from the upstream config, never used |

## Editors

vim 2016 was a 504-line vimrc; VS Code is the primary editor today (its
settings/keybindings live in the private layer), Neovim/LazyVim is the fast
terminal editor.

| 2016 habit | Destination | Status |
|---|---|---|
| `leader-w` save (leader now Space) | nvim keymaps + VS Code | PORTED |
| `C-n` relative-number toggle | nvim keymaps.lua | RESTORED |
| `Space` = search | nvim keymaps.lua | PORTED |
| fast scroll `C-e`/`C-y` ×3 (+`M`) | nvim keymaps.lua | PORTED |
| WinMove `C-hjkl` auto-split | LazyVim native | NATIVE |
| move lines `M-j`/`M-k` | LazyVim native | NATIVE |
| strip trailing whitespace on write | autocmds.lua (C++/Python) | PORTED |
| re-indent file `leader-i` | `leader-cf` (conform) | NATIVE |
| visible listchars | options.lua pinned | RESTORED |
| visual `*`/`#` | nvim keymaps.lua | RESTORED |
| buffers `leader-l/h` | `S-h`/`S-l` | NATIVE |
| alternate file `leader-.` | `leader-bb` | NATIVE |
| tab management (`tn/to/tc/tm/tl`) | — | DROPPED | buffers are the LazyVim model |
| jellybeans + airline | LazyVim default theme | ADAPTED |
| NERDTree `leader-k/y` | neo-tree `leader-e/fe` | NATIVE |
| fzf mappings + insert completions | telescope/LazyVim pickers | NATIVE |
| YouCompleteMe `yd/fi/gt` | clangd LSP | ADAPTED |
| spell leader mappings | — | DROPPED | `z=` native; VS Code spell-checker |
| `W` sudo-write | — | DROPPED | marginal with VS Code primary |
| scratch buffers `leader-q/x` | — | DROPPED | 2016 workflow |
| `leader-pp` paste toggle | — | DROPPED | bracketed paste |
| `0` → `^` | — | DROPPED | muscle memory not carried to VS Code |
| AZERTY digit swaps | — | DROPPED | QWERTY since 2026 |
| YCM-Generator, syntastic, vim-signify, sleuth, indentLine, nerdcommenter, surround, fugitive | LazyVim distribution equivalents | ADAPTED |

## C++ toolchain / packages / AI plumbing

| Source (chezmoi) | Destination | Status |
|---|---|---|
| clang-format / clang-tidy / ccache.conf | `cpp/*.symlink` | PORTED verbatim |
| `cpp-new` generator | `bin/cpp-new` | PORTED |
| vcpkg bootstrap (`.vcpkg` flag) | `cpp/install.sh` + `DOTFILES_VCPKG=1` | ADAPTED |
| apt lists + installer (run_onchange) | `apt/packages` + `apt/install.sh` (hash-gate) | ADAPTED | +shellcheck/duf/dust/xclip |
| `uv` curl-installer (chezmoi apt script) | — | DROPPED | install on demand (`brew install uv` / astral script) — was an implicit side-effect, now a conscious choice |
| VS Code extensions (18 generic) | `vscode/install.sh` | PORTED | opt-out flag |
| format-cpp Claude hook | `claude/home/.claude/hooks/format-cpp.sh.symlink` | PORTED |
| claude settings example | `claude/settings.example.json` | PORTED | generic placeholders only |
| Brewfile | `Brewfile` | PORTED | −chezmoi, +shellcheck/duf/dust |
| neovim-linux tarball installer | `nvim/install.sh` | PORTED | pin v0.12.5 inline |

## Infrastructure (chezmoi mechanisms → topic mechanisms)

| chezmoi | Topic model |
|---|---|
| `run_onchange_*` scripts + content hashes | idempotent `install.sh` + `script/gate.sh` hash-gates (brew, apt, private MCP) |
| templates + `.chezmoi.os` branches | static files + `darwin/`/`linux/` link-time subtrees + runtime `command -v` guards |
| `.chezmoiexternal.toml` pinned tarballs | pinned git clones (`zsh/install.sh`) with drift repair |
| `.chezmoidata/versions.yaml` | pins inline in each `install.sh` (single place per topic) |
| `chezmoi apply` / `chezmoi diff` / doctor | `script/bootstrap [--dry-run]` / `ls -l ~` / CI smoke job |
| CI template-render job | gone — no templates to render |

## Private layer

A private overlay repo (same topic model, applied after this one) holds the
personal, non-publishable state — full editor settings, AI-assistant policy,
machine-specific configs. Its contents are deliberately not enumerated here;
see its own README. One known cosmetic drift from the adoption: the editor
files still carry "managed by chezmoi" header comments — clean them up on the
next natural edit.
